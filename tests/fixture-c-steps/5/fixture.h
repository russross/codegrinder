#ifndef FIXTURE_H
#define FIXTURE_H

#include <stddef.h>

int fixture_one(int value);
int fixture_two(int left, int right);
int fixture_three(const int *values, size_t count);
int fixture_four(int value);
int fixture_five(int value);

#endif
